# Contributing

欢迎中文或英文 issue / PR。请先阅读 README 和数据政策。此项目不接受收益保证或未经授权的第三方数据转存。

1. 描述可复现问题、预期/实际行为、Node 版本和系统；截图前移除账户和个人记录。
2. 使用 `npm ci --include=dev --include=optional` 安装依赖。
3. 在小范围分支修改，为逻辑修复添加合成测试。不要用维护者私人档案作测试前提。
4. 运行 `npm test`、`npm run audit:release`、`npm run demo:backtest`、`npm run build`。
5. PR 说明数据来源、公式、单位、日期口径、交易假设和局限；不要只贴“收益提升”。

新增策略至少检查未来信息泄漏、现金/持仓对账、交易成本、退出机制和未成交情况。选股池、参数搜索和样本外区间应事先说明。

不要提交 `.env`、数据库、真实订单、访问凭据、行情缓存或有版权全文。合成样本须明确标记 synthetic，不能假称真实行情。

贡献内容需是你有权提交的内容，并同意以项目 MIT 许可贡献；保留第三方原有声明。CI 不代表全面金融模型审计或安全认证。
